langgraph>=0.1.0
langchain-google-genai>=1.0.0
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
requests>=2.31.0
fastembed

# Local packages (installed via pyproject.toml / pip install -e .)
# opticargo-shared
# opticargo-rag-pipeline
# opticargo-knowledge-graph

groq